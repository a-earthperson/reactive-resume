/**
 * @packageDocumentation
 *
 * @remarks
 * Barrel module for Resume data. This re-exports the DraftResume data contracts
 * so resume data stays aligned with draft data during view composition.
 *
 * @see {@link ../../draft/data | DraftResume data}
 */

export * from "../../draft/data";
